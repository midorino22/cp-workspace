# Copy to .env and adjust for your host.
COMPOSE_PROJECT_NAME=cp

# Host <-> container port mapping for local visualizers / web tools.
VIS_PORT_HOST=8080
VIS_PORT_CONTAINER=8080

# Non-root user numeric identity inside container.
CONTAINER_USER_UID=1000
CONTAINER_USER_GID=1000
